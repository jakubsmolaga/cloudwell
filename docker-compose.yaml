version: "3.9"
services:
  mqtt-broker:
    image: "eclipse-mosquitto"
    ports:
      - "1883:1883"
    volumes:
      - ./mosquitto.conf:/mosquitto/config/mosquitto.conf
  datacollector:
    build: ./datacollector

volumes:
  mosquitto: